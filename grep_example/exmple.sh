grep 'sdfas' hi.txt
grep -r 'sd' ~/Documents/BASH/grep_example/
#gives the line that has 'sdfas'
#use -i flag to ignore case
#use -r to search through all files and subdirectories in the current dir recursively.
#use -v to find lines that do NOT match the pattern.